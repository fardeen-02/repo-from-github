name: My First Workflow

on:
  push:

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello Fardeen! CI is working 🚀"

      - name: Print Date
        run: date
