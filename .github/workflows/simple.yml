name: My first workflow

on: 
  push:

jobs:
  say-hello:
   runs-on: ubuntu-latest

   steps:
     - uses: Print message
       run : echo "hello fardeen ,CI is working"

     - uses: print date
       run : date
     
